#include <stdio.h>
int count=1,first_num = 1,second_num = 1,sum=0,temp_var=0;


int main() {
	//code
	
	if(count>2)
	    temp_var=2;
	
	else
	    temp_var = count;
	
	for(int i=0; i<temp_var; i++){
	    printf("1\n\r");
	}
	
	
	for(int i=2;i<count;i++)
	{
	    sum = first_num+second_num;
	    printf("%d\n\r",sum);
	    second_num = first_num;
	    first_num = sum;
	    
	}
	return 0;
}